TOKENIZER -----------------------------
  cancel unclosed text after escape on first line
  cancel unclosed comment on first line
  decimal numbers
  hex numbers
  hex decimals
  hex escapes
  nested comment
  documentation comments


PARSER -----------------------------
! cleanup and reliability fixes
! generate remarks
! coma separated statements and struct members
! new line separated fn args and params
  parse array / brace op apply
  comments
  operators
  include all tokens incl white with exp objects


VALIDATOR -----------------------------
  syntax
  intendation level


PREPARER -----------------------------
! prepare functions on firs call
  remark undefined identifiers
  remark multiply defined identifiers
  remark unused identifiers
  operators priority
  tail call


TYPE INFERENCE / CHECK -----------------------------
! infer identfier by usage in fn (not only by def)
! infer identifier by usage as fn arg
  report type errors
  report declared vs inffered type dicrepancies
  infer definite assigment
  infer value origin
  infer value destination
  infer possible values for variable
  infer on fn apply / op apply: throws, ...
  infer on fn pure, deterministic throws, modify global, ..


INTERPRETER -----------------------------
! struct
! builtins with qualified name
! dynamic and static typing versions
  array object (empty, first, rest)
    variable length fn params
    text is array
  var / mutable variables
  ref
  throw
  try catch finally /, scope guars
  contracts
  builtin for stack trace
  builtin in for env
  user overridable struct functions - construct, destruct, clone, ...
  cast
  arrays / mem allocation


FORMATTER -----------------------------
  configuration profiles


AUTFIXER -----------------------------
  formatting / syntax


CMD LINE -----------------------------
! project file, multi file support


INTERACTIVE -----------------------------
  first version