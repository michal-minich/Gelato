start = fn ()
{
	label again
	print("enter number")
	ln = readln()
	n = toNum(ln)
	if n === 5 then
		print ("ok")
	else
		print (n, " is not 5")
		goto again
	end
}

start = fn ()
{
	a = 1
	label again
	n = inc(a)
	if n === 5 then
		print ("ok")
	else
		print (n, " is not 5")
		goto again
	end
}